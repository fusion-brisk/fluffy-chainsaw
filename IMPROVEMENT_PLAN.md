# План улучшения обратной связи в UI

## Проблемы, которые нужно решить

### 1. Анимация серых точек не очень заметна
**Текущее состояние:**
- Маленькие серые точки (5px)
- Низкая контрастность
- Анимация pulse с opacity 0.4-1.0

**Решение:**
- Увеличить размер точек до 6-7px
- Использовать цвет бренда (#5865F2) вместо серого
- Усилить анимацию (больше контраст, может быть bounce)
- Добавить пульсацию фона индикатора

### 2. Вводящие в заблуждение сообщения в логах
**Проблемные сообщения:**
- `✅ Извлечено ${rows.length} результатов из HTML` (строка 208, 219 в ui.tsx)
- Создают ложное впечатление, что работа завершена

**Решение:**
- Убрать эмодзи ✅ из сообщений о начале обработки
- Изменить формулировку: "Извлечено" → "Найдено" или "Подготовлено"
- Использовать эмодзи только для финального завершения
- Добавить префикс "Этап 1/4:" для ясности

### 3. Процент прогресса показывает 0 или сразу 100
**Текущее состояние:**
- Прогресс устанавливается только при чтении файла: `{ current: 0, total: 100, message: 'Чтение файла...' }`
- После этого не обновляется до завершения
- Пользователь видит 0% или сразу 100%, не видит промежуточных этапов

**Решение:**
- Добавить отправку прогресса из `code.ts` на каждом этапе:
  - **Этап 1 (0-20%)**: Поиск контейнеров
  - **Этап 2 (20-40%)**: Группировка сниппетов и создание layerData
  - **Этап 3 (40-60%)**: Обработка компонентной логики
  - **Этап 4 (60-80%)**: Обработка текста
  - **Этап 5 (80-100%)**: Обработка изображений (с обновлениями каждые 3-5 изображений)
- Обновлять прогресс в реальном времени во время обработки изображений
- Показывать детальную информацию: "Обработка изображений: 15/50"

## Детальный план реализации

### Этап 1: Улучшение визуализации анимации

**Файлы для изменения:**
- `src/styles.css` (строки 495-522)

**Изменения:**
```css
.status-thinking-dot {
  width: 7px;  /* было 5px */
  height: 7px;  /* было 5px */
  background: var(--figma-color-bg-brand);  /* было var(--figma-color-text-secondary) */
  border-radius: 50%;
  animation: pulse-bright 1.2s ease-in-out infinite;
  box-shadow: 0 0 4px rgba(88, 101, 242, 0.5);  /* свечение */
}

@keyframes pulse-bright {
  0%, 100% { 
    opacity: 0.5;
    transform: scale(1);
  }
  50% { 
    opacity: 1;
    transform: scale(1.2);
  }
}

.status-thinking-indicator {
  background: rgba(88, 101, 242, 0.1);  /* легкий фон */
  border: 1px solid rgba(88, 101, 242, 0.2);
}
```

### Этап 2: Исправление вводящих в заблуждение сообщений

**Файлы для изменения:**
- `src/ui.tsx` (строки 208, 219)

**Изменения:**
```typescript
// БЫЛО:
addLog(`✅ Извлечено ${rows.length} результатов из MHTML`);
addLog(`✅ Извлечено ${rows.length} результатов из HTML`);

// СТАНЕТ:
addLog(`📋 Найдено ${rows.length} результатов в файле (начало обработки...)`);
// или
addLog(`📋 Подготовлено ${rows.length} строк данных для обработки`);
```

**Дополнительно:**
- Убрать ✅ из всех промежуточных сообщений
- Оставить ✅ только для финального "Готово!"
- Добавить префиксы этапов: "Этап 1/5: Поиск контейнеров..."

### Этап 3: Добавление реального прогресса

**Файлы для изменения:**
- `src/code.ts` - добавить отправку прогресса на каждом этапе
- `src/image-handlers.ts` - добавить прогресс при обработке изображений
- `src/ui.tsx` - обработать новые сообщения прогресса

**Детальный план:**

#### 3.1. Прогресс в code.ts

Добавить отправку прогресса после каждого этапа:

```typescript
// После поиска контейнеров (строка ~333)
figma.ui.postMessage({
  type: 'progress',
  current: 20,
  total: 100,
  message: `Найдено ${allContainers.length} контейнеров`,
  operationType: 'searching'
});

// После группировки сниппетов (строка ~412)
figma.ui.postMessage({
  type: 'progress',
  current: 40,
  total: 100,
  message: `Создано ${snippetGroups.size} групп сниппетов`,
  operationType: 'grouping'
});

// После обработки компонентной логики (строка ~579)
figma.ui.postMessage({
  type: 'progress',
  current: 60,
  total: 100,
  message: `Обработана компонентная логика`,
  operationType: 'components'
});

// После обработки текста (строка ~604)
figma.ui.postMessage({
  type: 'progress',
  current: 70,
  total: 100,
  message: `Обработано ${textLayers.length} текстовых слоев`,
  operationType: 'text'
});

// Перед обработкой изображений (строка ~611)
figma.ui.postMessage({
  type: 'progress',
  current: 75,
  total: 100,
  message: `Начинаем обработку ${imageLayers.length} изображений...`,
  operationType: 'images-start'
});
```

#### 3.2. Прогресс в image-handlers.ts

Добавить обновления прогресса во время обработки изображений:

```typescript
// В методе processPool (строка ~508)
public async processPool(items: LayerDataItem[]): Promise<void> {
  Logger.info('🔄 Начинаем обработку пула изображений...');
  
  // Отправляем начальный прогресс
  figma.ui.postMessage({
    type: 'progress',
    current: 75,
    total: 100,
    message: `Обработка изображений: 0/${items.length}`,
    operationType: 'images'
  });
  
  let processedCount = 0;
  const total = items.length;
  
  // В цикле обработки (после processImage)
  processedCount++;
  
  // Обновляем каждые 3 изображения или каждые 5%
  if (processedCount % 3 === 0 || processedCount % Math.max(1, Math.floor(total / 20)) === 0) {
    const progress = 75 + Math.floor((processedCount / total) * 25); // 75-100%
    figma.ui.postMessage({
      type: 'progress',
      current: progress,
      total: 100,
      message: `Обработка изображений: ${processedCount}/${total}`,
      operationType: 'images'
    });
  }
}
```

#### 3.3. Улучшение LiveProgressView

Добавить отображение детального прогресса:

```typescript
// Показывать текущий этап и детали
{progress?.operationType && (
  <div className="status-thinking-stage">
    {getStageLabel(progress.operationType)}: {progress.message}
  </div>
)}
```

### Этап 4: Улучшение визуализации прогресса в DropZone

**Файлы для изменения:**
- `src/components/DropZone.tsx`
- `src/styles.css`

**Изменения:**
- Увеличить высоту прогресс-бара с 3px до 4-5px
- Добавить анимацию заполнения
- Показывать процент более заметно

## Приоритет реализации

1. **Высокий приоритет:**
   - Исправление вводящих в заблуждение сообщений (быстро, важно для UX)
   - Добавление реального прогресса (критично для понимания работы)

2. **Средний приоритет:**
   - Улучшение анимации точек (улучшает восприятие)

3. **Низкий приоритет:**
   - Дополнительные визуальные улучшения

## Ожидаемый результат

После реализации:
- ✅ Пользователь видит заметную анимацию работы
- ✅ Логи не создают ложного впечатления о завершении
- ✅ Прогресс обновляется плавно от 0% до 100% с детализацией этапов
- ✅ Видно текущий этап обработки и количество обработанных элементов
- ✅ При обработке большого количества инстансов видно реальный прогресс
