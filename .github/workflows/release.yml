name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: native-host/package-lock.json

      - name: Install dependencies for native-host
        run: npm ci
        working-directory: native-host

      - name: Build native binaries
        run: |
          npm run build
          # Rename binaries for clarity
          mv dist/eproductsnippet-relay-host-macos-arm64 dist/relay-host-arm64 || true
          mv dist/eproductsnippet-relay-host-macos-x64 dist/relay-host-x64 || true
          mv dist/eproductsnippet-relay-host-macos dist/relay-host-x64 || true
          # Make executable
          chmod +x dist/relay-host-*
        working-directory: native-host

      - name: Package Chrome Extension
        run: |
          cd extension
          zip -r ../Extension.zip . -x "*.DS_Store" -x "__MACOSX/*"

      - name: Copy installer script
        run: |
          cp scripts/EProductSnippet-Installer.command ./EProductSnippet-Installer.command
          chmod +x ./EProductSnippet-Installer.command

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            EProductSnippet-Installer.command
            Extension.zip
            native-host/dist/relay-host-arm64
            native-host/dist/relay-host-x64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
