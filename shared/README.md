# shared/

Shared types and utilities used across packages (plugin, extension, relay).

## Status

**Placeholder** — will be populated in Phase 3 when extension and relay migrate to TypeScript.

## Planned contents

- `types/csv-fields.ts` — CSVFields interface, SnippetType enum (currently in `packages/plugin/src/types/`)
- `types/protocol.ts` — Relay ↔ Plugin ↔ Extension message types
- `utils/yandex-shared.ts` — Shared selectors and parsing utilities (currently in `packages/plugin/src/utils/`)
