# Contentify Plugin — Отладка и добавление данных

## Контекст

Figma-плагин **Contentify** (EProductSnippet) автоматически заполняет макеты данными из HTML/MHTML файлов Яндекс.Поиска.

**Завершённый рефакторинг (Phase 2-8):**
- `code.ts`: 1163 → 129 строк (entry-point)
- Модульная архитектура: handlers, plugin, types, utils
- 58 unit-тестов (Vitest)
- Streaming MHTML parser для больших файлов

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                         UI (ui.tsx)                              │
│  Drop HTML/MHTML → parseMhtmlStreamingAsync → parseYandexSearch │
│                           ↓                                      │
│                    CSVRow[] (данные)                             │
│                           ↓                                      │
│              postMessage('import-csv', rows)                     │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                      CODE (code.ts)                              │
│  message-router.ts → snippet-processor.ts                        │
│         ↓                    ↓                                   │
│  data-assignment.ts   handlerRegistry.executeAll(context)        │
│  (rows → containers)        ↓                                    │
│                       15+ handlers                               │
│                       (button, price, label, delivery, etc.)     │
└─────────────────────────────────────────────────────────────────┘
```

## Ключевые файлы

### Парсинг HTML → CSVRow[]

| Файл | Назначение |
|------|------------|
| `src/utils/snippet-parser.ts` | Главный парсер HTML → CSVRow[], extractRowData() |
| `src/utils/mhtml-streaming.ts` | Streaming MHTML parser |
| `src/utils/favicon-extractor.ts` | Извлечение фавиконок (спрайты, CSS) |
| `src/utils/price-extractor.ts` | Извлечение цен |
| `src/utils/json-parser.ts` | Парсинг JSON из noframes |
| `src/utils/css-cache.ts` | Кэширование CSS правил |
| `src/utils/dom-cache.ts` | Кэширование DOM запросов |

### Типы данных

| Файл | Назначение |
|------|------------|
| `src/types/csv-fields.ts` | **CSVFields** — ~40 типизированных полей (#OrganicTitle, #ShopName, etc.) |
| `src/types/validation.ts` | validateRow, hasRequiredFields |
| `src/handlers/types.ts` | HandlerContext, HandlerResult |

### Применение данных к Figma

| Файл | Назначение |
|------|------------|
| `src/plugin/snippet-processor.ts` | Главный цикл: containers → handlers → text/images |
| `src/plugin/data-assignment.ts` | Маппинг CSVRow[] → containers |
| `src/handlers/registry.ts` | HandlerRegistry — централизованный вызов handlers |
| `src/handlers/price-handlers.ts` | EPriceGroup, EPrice, LabelDiscount |
| `src/handlers/button-handlers.ts` | EButton, MarketCheckoutButton |
| `src/handlers/label-handlers.ts` | Brand, ELabelGroup, EPriceBarometer |
| `src/handlers/delivery-handlers.ts` | EDeliveryGroup, BNPL |
| `src/handlers/snippet-handlers.ts` | Fallbacks, OfficialShop, EOfferItem |
| `src/handlers/field-fallbacks.ts` | Декларативные fallback chains |

## Поток данных

### 1. Парсинг HTML

```
HTML/MHTML файл
    ↓
parseMhtmlStreamingAsync() — извлечение HTML из MHTML
    ↓
parseYandexSearchResults(html, fullMhtml)
    ↓
findSnippetContainers() — поиск .EShopItem, .EProductSnippet2, etc.
    ↓
extractRowData(container) — для каждого контейнера
    ↓
CSVRow[] — массив строк с полями #OrganicTitle, #ShopName, #OrganicPrice, etc.
```

### 2. Применение к Figma

```
CSVRow[] (от UI через postMessage)
    ↓
findSnippetContainers('page' | 'selection') — поиск Figma-контейнеров
    ↓
assignRowsToContainers() — сопоставление по типам (EShopItem→EShopItem)
    ↓
handlerRegistry.executeAll(context) — для каждого контейнера
    ↓
15+ handlers в порядке приоритетов:
  CRITICAL(0): EPriceGroup
  VARIANTS(10): BrandLogic, EPriceBarometer, EButton...
  VISIBILITY(20): OfficialShop
  TEXT(30): LabelDiscountView, EDeliveryGroup
  FALLBACK(40): OrganicText fallbacks
    ↓
processTextLayers() — текстовые слои
    ↓
imageProcessor.processPool() — изображения
```

## Типы сниппетов

```typescript
type SnippetType = 
  | 'EShopItem'           // Карточка товара в выдаче
  | 'EOfferItem'          // Предложение магазина (popup)
  | 'EProductSnippet2'    // Товарный сниппет v2
  | 'EProductSnippet'     // Товарный сниппет v1
  | 'ProductTile-Item'    // Плитка товара
  | 'Organic_withOfferInfo' // Органика с ценой
  | 'Organic'             // Органика без цены
  | 'ESnippet'            // Универсальный сниппет
  | 'Snippet';            // Базовый сниппет
```

## Основные поля CSVRow

```typescript
// Идентификация
'#SnippetType': SnippetType
'#ProductURL': string

// Контент
'#OrganicTitle': string      // Заголовок
'#OrganicText': string       // Описание
'#OrganicHost': string       // Домен
'#OrganicImage': string      // URL картинки товара

// Магазин
'#ShopName': string          // Название магазина
'#FaviconImage': string      // URL фавиконки
'#OfficialShop': 'true'|'false'

// Рейтинг
'#ProductRating': string     // "4,5"
'#ReviewsNumber': string     // "1234 отзыва"

// Цены
'#OrganicPrice': string      // "1 990 ₽"
'#OldPrice': string          // "2 490 ₽"
'#DiscountPercent': string   // "-20%"

// Компоненты
'#EPriceGroup_Discount': 'true'|'false'
'#EPriceGroup_Fintech': 'true'|'false'
'#EPrice_View': 'default'|'special'
'#BUTTON': 'true'|'false'
'#ButtonView': 'primaryLong'|'primaryShort'|'secondary'|'white'
'#EDeliveryGroup': 'true'|'false'
'#DeliveryList': string
```

## Команды

```bash
cd /Users/shchuchkin/Documents/GitHub/fluffy-chainsaw

npm run build      # Сборка плагина
npm run test       # Запуск тестов (58 тестов)
npm run lint       # Проверка ESLint
```

## Типичные задачи отладки

### 1. Данные не извлекаются из HTML

Проверить:
- `src/utils/snippet-parser.ts` — селекторы в extractRowData()
- `config/parsing-rules.json` — конфиг парсинга
- Console.log в extractRowData() для диагностики

### 2. Данные не применяются к Figma

Проверить:
- `src/plugin/data-assignment.ts` — маппинг row → container
- `src/handlers/*.ts` — соответствующий handler
- Logger.info/debug в snippet-processor.ts

### 3. Добавление нового поля данных

1. Добавить в `src/types/csv-fields.ts` → CSVFields
2. Добавить извлечение в `src/utils/snippet-parser.ts` → extractRowData()
3. (Опционально) Добавить handler в `src/handlers/`
4. Зарегистрировать в `src/handlers/registry.ts`

### 4. Добавление нового типа сниппета

1. Добавить в `src/types/csv-fields.ts` → SnippetType
2. Добавить селекторы в `src/utils/snippet-parser.ts`
3. Добавить в `src/utils/container-search.ts` → CONTAINER_NAMES
4. Обновить маппинг в `src/plugin/data-assignment.ts`

## Полезные логи

При импорте в консоли Figma:
```
📊 Получено N строк данных
📦 Найдено N контейнеров-сниппетов
📍 [0] EShopItem: Shop="...", Price="...", Fintech=true
✅ [container-id] 15/15 handlers успешно
```

При парсинге в консоли браузера:
```
🔍 HTML разбор начат
📦 Контейнеров для обработки: N
✅ Извлечен EShopItem: ...
```

## Правила (из .cursorrules)

- ES5 target для code.js (Figma Plugin API)
- Никаких Node.js API в рантайме
- При изменениях ui.html — отдавать полный файл
- Логирование обязательно

## Начало работы

1. Опиши проблему или задачу
2. Приложи пример HTML (если есть)
3. Укажи тип сниппета (EShopItem, EOfferItem, etc.)
4. Опиши ожидаемое vs фактическое поведение



