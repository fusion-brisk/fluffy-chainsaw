# 📊 Руководство по структуре CSV для EProductSnippet

## ✅ Проблема решена!

Плагин теперь автоматически обрабатывает **все поля** из CSV файла, а не только `layer` и `value`.

## 🔍 Как это работает

### Старая логика (не работала):
- Искал поля `layer` и `value`
- Ваши данные содержат поля типа `#OrganicTitle`, `#OrganicPrice` и т.д.

### Новая логика (работает):
- Обрабатывает **все поля** из CSV
- Ищет слои с именами, соответствующими названиям полей
- Заполняет найденные слои значениями из полей

## 📋 Структура ваших данных

Из логов видно, что у вас есть поля:
- `#OrganicTitle` - название товара
- `#OrganicPrice` - цена
- `#ShopName` - название магазина
- `#ShopRating` - рейтинг
- `#ReviewsNumber` - количество отзывов
- `#ThumbImage` - изображение товара
- И многие другие...

## 🎯 Как использовать

### 1. Создайте слои в Figma
Создайте текстовые слои с именами, соответствующими полям CSV:
- `#OrganicTitle` - для названия товара
- `#OrganicPrice` - для цены
- `#ShopName` - для названия магазина
- `#ShopRating` - для рейтинга
- `#ReviewsNumber` - для количества отзывов

### 2. Для изображений
Создайте прямоугольники или другие фигуры с именами:
- `#ThumbImage` - для изображения товара
- `#OrganicImage` - для основного изображения

### 3. Запустите плагин
- Выберите лист с данными
- Выберите область (Selection/Page/Document)
- Нажмите "Заполнить"

## 🔍 Отладка

### В консоли Figma вы увидите:
```
🔍 Обрабатываем строку данных: {#OrganicTitle: "Кресло", #OrganicPrice: "140 000", ...}
🔍 Обрабатываем поле: "#OrganicTitle" = "Кресло"
✅ Найден слой "#OrganicTitle" типа TEXT
✅ Обновлен текстовый слой "#OrganicTitle": "Кресло"
```

### Если слой не найден:
```
❌ Слой "#OrganicTitle" не найден среди доступных элементов
```

**Решение:** Создайте слой с точным именем `#OrganicTitle`

## 🎨 Пример макета

Создайте в Figma:
```
📦 Карточка товара
  ├── #OrganicTitle (текст)
  ├── #OrganicPrice (текст)
  ├── #ShopName (текст)
  ├── #ShopRating (текст)
  ├── #ReviewsNumber (текст)
  └── #ThumbImage (прямоугольник)
```

## 🚀 Тестирование

1. **Создайте тестовый слой** с именем `#OrganicTitle`
2. **Выберите его** (для области Selection)
3. **Запустите плагин** с вашими данными
4. **Проверьте консоль** - должны появиться логи обработки

## 📝 Важные моменты

- **Точные имена** - названия слоев должны точно совпадать с названиями полей CSV
- **Типы слоев** - текстовые поля для текста, фигуры для изображений
- **Область применения** - выберите правильную область (Selection/Page/Document)
- **Фильтр** - можно использовать для обработки только определенных полей

Теперь плагин должен работать с вашими данными! 🎉
